# Training Workflows

RESP fitting workflows for training charge models.

Use the CLI with `--train resp.<module>`.

Example:

```bash
biliresp --train resp.twostepRESP_basic --yaml HID/twostepRESP_basic
```

See the RESP workflows below for details.
